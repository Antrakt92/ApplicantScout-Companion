# Developer/backcompat config. Normal installed users should use the companion
# first-run/settings UI, which writes:
# %LOCALAPPDATA%\applicant-scout\config\config.env
#
# Get these from https://www.warcraftlogs.com/api/clients/
# Create a client with Redirect URL exactly http://localhost and Public Client
# unchecked.
WCL_CLIENT_ID=
WCL_CLIENT_SECRET=

# Optional overrides
# Primary transport folder override. Must be the active WoW retail Screenshots
# folder under _retail_; arbitrary folders are rejected because the addon writes
# transport snapshots through WoW's screenshot system.
# APSCOUT_SCREENSHOTS_PATH=C:\Games\World of Warcraft\_retail_\Screenshots

# Startup fallback only; live snapshots from the addon normally provide region.
# APSCOUT_REGION=EU

# Optional WCL character cache TTL in seconds. Blank/unset keeps the 12-hour
# default.
# APSCOUT_CACHE_TTL_SECONDS=43200

# WCL data scope. Accepted booleans: 1/0, true/false, yes/no, on/off.
# All scopes default to 1 when unset; saved 0 values remain disabled.
# Invalid APSCOUT_FETCH_* values stop startup instead of silently changing
# Warcraft Logs quota scope.
# APSCOUT_FETCH_MPLUS=1
# APSCOUT_FETCH_RAID_NORMAL=1
# APSCOUT_FETCH_RAID_HEROIC=1
# APSCOUT_FETCH_RAID_MYTHIC=1

# Optional WoW lifecycle sync. 1 starts/stops the companion with the local WoW
# session and adds a Windows sign-in helper; 0 leaves launch/quit manual.
# Defaults to 1 when unset; a saved 0 remains disabled.
# APSCOUT_SYNC_WITH_WOW=1
